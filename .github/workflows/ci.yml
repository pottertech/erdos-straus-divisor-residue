name: CI (Lean build + certificate verification + audit)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lean-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Lean via elan
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y --default-toolchain none
          source $HOME/.elan/env
          lake --version
      - name: Build Lean project
        run: |
          source $HOME/.elan/env
          lake build

  certificate-verification:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - name: Verify certificates (per-record)
        run: python analysis/layer4/verify_certificates.py
      - name: Check certificate dataset integrity
        run: python analysis/layer4/check_certificate_dataset.py

  audit:
    runs-on: ubuntu-latest
    needs: [lean-build, certificate-verification]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - name: Install Lean via elan
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y --default-toolchain none
          source $HOME/.elan/env
      - name: Run full audit (strict mode)
        run: |
          source $HOME/.elan/env
          bash scripts/audit_all.sh --strict